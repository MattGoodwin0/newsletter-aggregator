# --- UI & PDF Generation ---
HTML_TEMPLATE = """
<!DOCTYPE html>
<html>
<head>
    <style>
    
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;900&family=Playfair+Display:ital,wght@0,700;1,700&display=swap');
        /* Cover Page */
        .cover { 
            background: #000; color: white; height: 100vh; 
            display: flex; flex-direction: column; justify-content: center; align-items: center; 
        }
        .cover h1 { font-family: 'Playfair Display', serif; font-size: 80px; margin: 0; letter-spacing: -2px; }
        .cover p { font-size: 20px; text-transform: uppercase; letter-spacing: 5px; color: #00d4ff; }
        
        body { margin: 0; padding: 0; background: #fff; font-family: 'Outfit', sans-serif; }
        .page { width: 210mm; height: 297mm; padding: 20mm; box-sizing: border-box; page-break-after: always; position: relative; border-left: 10px solid #000; }
        .header { border-bottom: 2px solid #000; padding-bottom: 5px; font-weight: 900; font-size: 11px; letter-spacing: 2px; display: flex; justify-content: space-between; }
        .article-title { font-size: 38px; font-weight: 900; line-height: 1; margin: 15px 0; letter-spacing: -1.5px; }
        .featured-image { width: 100%; height: 85mm; object-fit: cover; margin-bottom: 20px; background: #f9f9f9; }
        .content-body { font-family: 'Playfair Display', serif; font-size: 14px; line-height: 1.6; color: #222; }
        /* Adds space specifically between paragraphs inside .content-body */
        .para {
        margin-bottom: 1.5em; /* Adjust this value to increase/decrease space */
        }

        /* Optional: Remove margin from the very last paragraph to avoid extra bottom spacing */
        .para:last-child {
        margin-bottom: 0;
        }
        .footer { position: absolute; bottom: 15mm; font-size: 10px; color: #999; border-top: 1px solid #eee; width: calc(100% - 40mm); padding-top: 10px; }
        .magazine-spread {
  display: grid;
  grid-template-columns: 1fr 1fr; /* Two equal pages */
  min-height: 100vh;
  background-color: #f4f4f4;
}

/* Base Page Styling */
.page {
  padding: 60px;
  box-sizing: border-box;
}

/* Left Page (The Ad) */
.ad-page {
  background-color: #1a1a1a; /* Dark, premium background */
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  border-right: 1px solid #333; /* Visual "spine" of the magazine */
}

.ad-brand { letter-spacing: 5px; font-size: 12px; margin-bottom: 40px; opacity: 0.7; }
.ad-headline { font-family: 'Playfair Display', serif; font-size: 42px; margin-bottom: 20px; }
.ad-subtext { font-family: 'Arial', sans-serif; font-size: 14px; opacity: 0.8; max-width: 250px; margin: 0 auto 30px; }

.ad-price { font-family: 'Playfair Display', serif; font-size: 48px; margin-bottom: 30px; }
.ad-price .currency { font-size: 20px; vertical-align: top; }
.ad-price .period { font-size: 16px; opacity: 0.6; }

.ad-button {
  background: transparent;
  border: 1px solid #fff;
  color: #fff;
  padding: 12px 30px;
  cursor: pointer;
  letter-spacing: 2px;
  transition: 0.3s;
}

.ad-button:hover { background: #fff; color: #1a1a1a; }

/* Right Page (The Article) */
.right-page {
  background-color: #fff;
}
    </style>
</head>
<body>
<div class="cover">
        <p>Tech Weekly</p>
        <h1>THE BRIEF</h1>
        <p>2026</p>
    </div>
    {% for article in articles %}
     <div class="page ad-page">
     <section class="page left-page ad-page">
    <div class="ad-content">
      <p class="ad-brand">THE ARCHIVE</p>
      <h2 class="ad-headline">Elevate Your Perspective</h2>
      <p class="ad-subtext">Access exclusive long-form features and ad-free reading.</p>
      <div class="ad-price">
        <span class="currency">$</span>1.99<span class="period">/mo</span>
      </div>
      <button class="ad-button">GO PRO</button>
    </div>
  </section>
  </div>
    <div class="page">
        <div class="header">
            <span>Tech Weekly</span>
            <span>VOL. 2026 // {{ article.date }}</span>
        </div>
        <h1 class="article-title">{{ article.title }}</h1>
        {% if article.image %}<img src="{{ article.image }}" class="featured-image">{% endif %}
        <div class="content-body">
            {% for p in article.paragraphs %}
            <div class="para">{{ p }}</div>
            {% endfor %}
        </div>
        <div class="footer">Source: {{ article.source_url }}</div>
    </div>
   
    {% endfor %}
</body>
</html>
"""
